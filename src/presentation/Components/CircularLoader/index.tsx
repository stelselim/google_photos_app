import {  Spinner } from "native-base";
import React from "react";
import { KPrimaryAppColor } from "../../themes/colors";

export const CircularLoader = () => {
    return <Spinner color={KPrimaryAppColor} />
}